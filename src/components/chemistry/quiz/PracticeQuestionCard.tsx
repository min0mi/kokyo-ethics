'use client';
import React from 'react';
import { CheckCircle2, XCircle, ChevronRight } from 'lucide-react';
import { ChemicalText } from '@/components/chemistry/ChemicalFormula';
export function PracticeQuestionCard({prompt,options,selected,answered,correct,onSelect,onNext}:{prompt:string;options:string[];selected:string|null;answered:boolean;correct:string;onSelect:(x:string)=>void;onNext:()=>void}){return <div className="mx-auto w-full max-w-4xl space-y-2 text-xs"><div className="space-y-3 rounded-xs border border-gray-400 bg-white p-3.5 shadow-xs sm:p-4"><div className="flex items-center justify-between border-b border-gray-200 pb-1 text-[11px]"><span className="rounded-xs border border-red-200 bg-red-50 px-1.5 py-0.5 font-bold text-red-700">対応問題</span>{answered&&<button onClick={onNext} className="flex items-center gap-0.5 rounded-xs bg-red-600 px-2.5 py-0.5 text-[10px] font-bold text-white">次へ <ChevronRight className="h-3 w-3"/></button>}</div><h3 className="py-1 text-base font-bold leading-snug text-gray-900 sm:text-lg"><ChemicalText text={prompt}/></h3><div className="grid grid-cols-1 gap-2 sm:grid-cols-2">{options.map((option,idx)=>{const isSelected=selected===option,isCorrect=option===correct;let style='bg-gray-50 border border-gray-300 text-gray-900 hover:bg-gray-100';if(answered){if(isCorrect)style='bg-green-100 border-2 border-green-600 text-green-950 font-bold';else if(isSelected)style='bg-red-100 border-2 border-red-500 text-red-950 font-bold';else style='bg-gray-50 border border-gray-200 text-gray-400'}return <button key={idx} disabled={answered} onClick={()=>onSelect(option)} className={`w-full text-left rounded-xs p-3 transition text-xs flex items-center justify-between min-h-[46px] ${style}`}><span className="flex items-center gap-2"><span className="flex h-4 w-4 shrink-0 items-center justify-center rounded-xs border border-gray-400 bg-white text-[10px] font-bold">{idx+1}</span><span className="font-semibold leading-tight"><ChemicalText text={option}/></span></span>{answered&&(isCorrect?<CheckCircle2 className="h-4 w-4 text-green-700"/>:isSelected?<XCircle className="h-4 w-4 text-red-600"/>:null)}</button>})}</div>{answered&&<div className="border-t border-gray-200 pt-2"><button onClick={onNext} className="flex w-full items-center justify-center gap-1 rounded-xs bg-red-600 py-2.5 text-xs font-bold text-white">次へ (Space / Enter)<ChevronRight className="h-3.5 w-3.5"/></button></div>}</div></div>}
